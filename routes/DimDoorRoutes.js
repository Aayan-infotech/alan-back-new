const express = require('express');
const router = express.Router();
const { } = require('../controllers/DimDoorControllers');


router.post();
router.get();
router.delete();

module.exports = router;
